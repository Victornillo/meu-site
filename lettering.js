document.addEventListener('DOMContentLoaded', () => {
    new TypeIt(".animated",{
        speed: 100, 
        String: "front-end",
    }).go()
})
document.addEventListener('DOMContentLoaded', () => {
    new TypeIt(".animated1",{
        speed: 100, 
        String: "front-end",
    }).go()
})
